<div class="categories">
    <h4>Categorias</h4>
    {{for category in categories:}}
        <h6><a href="{{=URL(c='content', f='list_category.html', vars={'category':category.identifier})}}">{{=category.title}}</a></h6>
    {{pass}}
</div>